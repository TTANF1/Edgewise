# edgewise-web (planned)

Browser demo of the cutout pipeline: load an exported sprite sheet, run
decontamination in the browser (WASM/WebGPU Layer 1 + a semantic backend),
and preview before/after.

Nothing here yet. The `examples/desk8/index.html` in the Aseprite workspace
is the current static preview.
