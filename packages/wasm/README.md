# edgewise-wasm (planned)

WASM/WebGPU build of Layer 1 (deterministic graphics) so the cutout
pipeline can run fully in the browser, feeding the `apps/web` demo.

Nothing here yet — the Python implementation in `packages/core` is the
single source of truth for the algorithms until this lands.
